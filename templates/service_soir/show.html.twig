{% extends 'base.html.twig' %}

{% block title %}ServiceSoir{% endblock %}

{% block body %}
    <h1>ServiceSoir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_soir.id }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ service_soir.horaire ? service_soir.horaire|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_soir_index') }}">back to list</a>

    <a href="{{ path('app_service_soir_edit', {'id': service_soir.id}) }}">edit</a>

    {{ include('service_soir/_delete_form.html.twig') }}
{% endblock %}

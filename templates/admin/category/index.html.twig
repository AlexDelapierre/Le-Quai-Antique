{% extends 'admin/index.html.twig' %}

{% block title %}Category index{% endblock %}

{% block main %}

    <div class="h1Admin">
        <h1>Category index</h1>
        <a href="{{ path('app_category_new') }}" class="btn btn-success">Ajouter</a>
    </div>
    <div class="tableResponsive table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    {# <th>Id</th> #}
                    <th>Name</th>
                    <th>Slug</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr>
                    {# <td>{{ category.id }}</td> #}
                    <td>{{ category.name }}</td>
                    <td>{{ category.slug }}</td>
                    <td>
                        {# <a href="{{ path('app_category_show', {'id': category.id}) }}">show</a> #}
                        <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-primary">Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">Aucune catégorie</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

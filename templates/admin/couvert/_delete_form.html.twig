<form method="post" action="{{ path('app_couvert_delete', {'id': couvert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couvert.id) }}">
    <button class="btn">Delete</button>
</form>

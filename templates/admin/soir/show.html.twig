{% extends 'base.html.twig' %}

{% block title %}Soir{% endblock %}

{% block body %}
    <h1>Soir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soir.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ soir.time ? soir.time|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_soir_index') }}">back to list</a>

    <a href="{{ path('app_soir_edit', {'id': soir.id}) }}">edit</a>

    {{ include('soir/_delete_form.html.twig') }}
{% endblock %}

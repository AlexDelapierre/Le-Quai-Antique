{% extends 'base.html.twig' %}

{% block title %}Edit Formule{% endblock %}

{% block body %}
    <h1>Edit Formule</h1>

    {{ include('admin/formule/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_formule_index') }}">back to list</a>

    {{ include('admin/formule/_delete_form.html.twig') }}
{% endblock %}

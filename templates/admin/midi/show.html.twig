{% extends 'base.html.twig' %}

{% block title %}Midi{% endblock %}

{% block body %}
    <h1>Midi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ midi.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ midi.time ? midi.time|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_midi_index') }}">back to list</a>

    <a href="{{ path('app_midi_edit', {'id': midi.id}) }}">edit</a>

    {{ include('midi/_delete_form.html.twig') }}
{% endblock %}

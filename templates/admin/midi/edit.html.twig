{% extends 'admin/adminTemplate.html.twig' %}

{% block title %}Edit Midi{% endblock %}

{% block body %}
    <h1>Edit Midi</h1>

    {{ include('midi/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_midi_index') }}">back to list</a>

    {{ include('midi/_delete_form.html.twig') }}
{% endblock %}

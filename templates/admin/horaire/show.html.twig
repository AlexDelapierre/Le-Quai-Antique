{% extends 'base.html.twig' %}

{% block title %}Horaire{% endblock %}

{% block body %}
    <h1>Horaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horaire.id }}</td>
            </tr>
            <tr>
                <th>Monday</th>
                <td>{{ horaire.monday }}</td>
            </tr>
            <tr>
                <th>Tuesday</th>
                <td>{{ horaire.tuesday }}</td>
            </tr>
            <tr>
                <th>Wednesday</th>
                <td>{{ horaire.wednesday }}</td>
            </tr>
            <tr>
                <th>Thursday</th>
                <td>{{ horaire.thursday }}</td>
            </tr>
            <tr>
                <th>Friday</th>
                <td>{{ horaire.friday }}</td>
            </tr>
            <tr>
                <th>Saturday</th>
                <td>{{ horaire.saturday }}</td>
            </tr>
            <tr>
                <th>Sunday</th>
                <td>{{ horaire.sunday }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_horaire_index') }}">back to list</a>

    <a href="{{ path('app_horaire_edit', {'id': horaire.id}) }}">edit</a>

    {{ include('admin/horaire/_delete_form.html.twig') }}
{% endblock %}

{% extends 'admin/index.html.twig' %}

{% block title %}Edit Menu{% endblock %}

{% block main %}
    <div class="h1Admin">
        <h1>Menu</h1>
        <a href="{{ path('app_menu_index') }}" class="btn btn-outline-primary me-2">Retour</a>
        <div class="deleteButton">
            {{ include('admin/menu/_delete_form.html.twig') }}
        </div>
    </div>  

    {{ include('admin/menu/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}

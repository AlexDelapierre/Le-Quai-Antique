{% extends 'base.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Edit Plat</h1>

    {{ include('admin/plat/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plat_index') }}">back to list</a>

    {{ include('admin/plat/_delete_form.html.twig') }}
{% endblock %}

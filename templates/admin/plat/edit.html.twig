{% extends 'base.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Plat</h1>

    {{ include('admin/plat/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_plat_index') }}" class="btn btn-outline-primary">Retour à la liste</a>

    {{ include('admin/plat/_delete_form.html.twig') }}
{% endblock %}

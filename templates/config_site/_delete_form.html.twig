<form method="post" action="{{ path('app_config_site_delete', {'id': config_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ config_site.id) }}">
    <button class="btn">Delete</button>
</form>

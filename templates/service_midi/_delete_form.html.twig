<form method="post" action="{{ path('app_service_midi_delete', {'id': service_midi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_midi.id) }}">
    <button class="btn">Delete</button>
</form>

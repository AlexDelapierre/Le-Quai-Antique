{% extends 'base.html.twig' %}

{% block title %}ServiceMidi{% endblock %}

{% block body %}
    <h1>ServiceMidi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_midi.id }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ service_midi.horaire ? service_midi.horaire|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_midi_index') }}">back to list</a>

    <a href="{{ path('app_service_midi_edit', {'id': service_midi.id}) }}">edit</a>

    {{ include('service_midi/_delete_form.html.twig') }}
{% endblock %}
